# Bug Report Template

**XferDB Version:**
`xferdb version` output or "latest"

**Source Database:**
Type, version, connection string (remove password)

**Target Database:**
Type, version, connection string (remove password)

**Config File:**
(if using `--config`, share the YAML — redact passwords)

**Command Used:**
```bash
xferdb migrate [command]
```

**Expected Behavior:**
What should happen

**Actual Behavior:**
What actually happened

**Steps to Reproduce:**
1. 
2. 
3. 

**Additional Context:**
Any other relevant info (OS, logs, etc.)
